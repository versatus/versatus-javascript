export interface Input {
    accountInfo: any; // Replace 'any' with the actual type
    contractInput: {
      contractFn: string; // Replace 'string' with the actual type
      // Add other properties of contractInput here
    };
}